const adminRole = 'Admin';
const managerRole = 'Manager';
const userRole = 'User';

const dateOptions = {
  year: 'numeric',
  month: 'long',
  day: 'numeric',
};

const dateAndTimeOptions = {
  year: 'numeric',
  month: 'long',
  day: 'numeric',
  hour: '2-digit',
  minute: '2-digit',
};

export default {
  adminRole,
  managerRole,
  userRole,
  dateOptions,
  dateAndTimeOptions,
};
